# 🔖 Tab Manager Extension - Installation Guide

Browser Extension แบบ Toby สำหรับจัดการและ bookmark tabs ใน browser

## ✨ Features

- 📂 **View All Open Tabs** - เห็น tabs ทั้งหมดที่เปิดอยู่ใน browser window ปัจจุบัน
- ☑️ **Multi-Select Tabs** - เลือก tabs ได้หลายตัวพร้อมกัน
- 💾 **Save as Bookmark** - บันทึก tabs ที่เลือกเป็น bookmark พร้อมตั้งชื่อ
- ⭐ **Manage Bookmarks** - ดู จัดการ เปิด และลบ bookmarks ที่บันทึกไว้
- 📂 **Restore Tabs** - เปิด tabs ทั้งหมดใน bookmark กลับมาได้ทันที
- ❌ **Close Selected Tabs** - ปิด tabs ที่เลือกได้เลย
- 🎨 **Beautiful UI** - ออกแบบสวยงามแบบ modern

## 🚀 วิธีติดตั้ง Extension

### สำหรับ Chrome / Edge

1. **เปิดหน้า Extensions**
   - Chrome: ไปที่ `chrome://extensions/`
   - Edge: ไปที่ `edge://extensions/`

2. **เปิด Developer Mode**
   - Toggle สวิตช์ "Developer mode" ที่มุมบนขวา

3. **Load Extension**
   - คลิกปุ่ม "Load unpacked"
   - เลือกโฟลเดอร์ `/Users/testtorial/Documents/MCP/planning-tool`

4. **Pin Extension** (แนะนำ)
   - คลิกที่ไอคอน puzzle (Extensions) ใน toolbar
   - หา "Planning Tool - Tab Manager"
   - คลิกปุ่ม pin 📌

5. **เริ่มใช้งาน**
   - คลิกที่ไอคอน extension ใน toolbar
   - จะเห็น popup แสดง tabs ทั้งหมดที่เปิดอยู่

## 📖 วิธีใช้งาน

### บันทึก Tabs เป็น Bookmark

1. คลิกที่ extension icon
2. ใน tab "📂 Current Tabs" จะเห็น tabs ทั้งหมดที่เปิดอยู่
3. เลือก tabs ที่ต้องการ bookmark (ติ๊กถูก checkboxes)
4. ตั้งชื่อ bookmark ในช่อง input
5. คลิก "💾 Save Bookmark"

### เปิด Bookmark

1. คลิกที่ extension icon
2. ไปที่ tab "⭐ Saved"
3. เลือก bookmark ที่ต้องการ
4. คลิกปุ่ม "📂" เพื่อเปิด tabs ทั้งหมด

### ลบ Bookmark

1. ไปที่ tab "⭐ Saved"
2. คลิกปุ่ม "🗑️" ที่ bookmark ที่ต้องการลบ
3. Confirm การลบ

### ปิด Tabs ที่เลือก

1. ใน tab "📂 Current Tabs"
2. เลือก tabs ที่ต้องการปิด
3. คลิก "❌ Close Selected"

## 🎯 Use Cases

- **Work Projects** - บันทึก tabs ที่เกี่ยวข้องกับโปรเจคต่างๆ
- **Research** - รวบรวม tabs ที่กำลังวิจัยหาข้อมูล
- **Daily Routine** - บันทึก tabs ที่เปิดประจำทุกวัน
- **Shopping** - เก็บ tabs ของสินค้าที่สนใจ
- **Learning** - รวม tabs ของคอร์สเรียน/บทความ

## 🔧 Technical Details

- **Manifest Version**: 3
- **Permissions**:
  - `tabs` - อ่านข้อมูล tabs
  - `storage` - บันทึก bookmarks
  - `bookmarks` - จัดการ bookmarks
- **Storage**: Chrome Storage API (Local)
- **Framework**: Vanilla JavaScript (ไม่มี dependencies)

## 📝 Notes

- Bookmarks จะถูกเก็บใน browser local storage
- Extension ทำงานเฉพาะ window ปัจจุบัน
- รองรับ Chrome, Edge, และ browsers ที่ใช้ Chromium

## 🎨 สร้างไอคอน (Optional)

ตอนนี้ยังไม่มีไอคอน ถ้าต้องการสร้าง:

1. สร้างไฟล์ PNG ขนาด 16x16, 48x48, 128x128 pixels
2. วางไว้ใน `/assets/icons/`
3. ตั้งชื่อเป็น `icon16.png`, `icon48.png`, `icon128.png`

หรือใช้ emoji เป็นไอคอนชั่วคราวได้

## 🐛 Troubleshooting

### Extension ไม่ทำงาน
- ตรวจสอบว่าเปิด Developer Mode แล้ว
- ลอง Reload extension
- เปิด DevTools ดู console errors

### Bookmarks หาย
- ตรวจสอบ Chrome Storage ใน DevTools
- อาจถูก clear storage ไป

### Tabs ไม่แสดง
- Refresh popup โดยปิดเปิดใหม่
- ตรวจสอบ permissions ใน manifest.json

## 📞 Support

สำหรับปัญหาหรือคำแนะนำ กรุณาเปิด issue ใน repository
