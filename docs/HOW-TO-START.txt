╔═══════════════════════════════════════════════════════════╗
║      📋 Planning Tool - วิธีเปิดใช้งานที่ถูกต้อง          ║
╚═══════════════════════════════════════════════════════════╝

⚠️  สำคัญมาก! ⚠️

❌ ห้ามทำ:
   - ห้าม double-click ไฟล์ .html โดยตรง
   - ห้ามเปิดผ่าน Finder
   - ห้ามใช้ file:/// protocol

✅ ต้องทำ:
   วิธีที่ 1: ใช้ Start Script (ง่ายที่สุด)
   ────────────────────────────────────────
   1. เปิด Terminal
   2. รันคำสั่ง:
      cd /Users/testtorial/Documents/MCP/planning-tool
      chmod +x start-all.sh
      ./start-all.sh
   
   3. รอจนเห็นข้อความ "System Ready"
   4. Browser จะเปิดให้อัตโนมัติ

   วิธีที่ 2: Start แบบ Manual
   ────────────────────────────────────────
   Terminal 1:
      cd /Users/testtorial/Documents/MCP/planning-tool
      python3 -m http.server 3000

   Terminal 2:
      python3 ai-backend.py

   Terminal 3:
      ollama serve

   แล้วเปิด: http://localhost:3000/start.html

🌐 URLs ที่ใช้ได้:
   🏠 Start Page:   http://localhost:3000/start.html
   🤖 AI Agent:     http://localhost:3000/ai-agent.html
   📋 Tasks:        http://localhost:3000/index.html

🔍 เช็คว่า Start สำเร็จหรือไม่:
   curl http://localhost:3000       # Web Server
   curl http://localhost:8001       # AI Backend
   curl http://localhost:11434      # Ollama

💡 Tips:
   - Bookmark: http://localhost:3000/start.html
   - ใช้ Start Page เป็นหน้าแรก
   - อย่าปิด Terminal ขณะใช้งาน

❓ ถ้ามีปัญหา:
   - Hard refresh: Cmd+Shift+R (Mac)
   - เปิด Console: F12 หรือ Cmd+Option+I
   - ดู error messages

📞 Need Help?
   - ตรวจสอบ logs: tail /tmp/*.log
   - Restart: ./start-all.sh
